# Configuración SMTP (rellena y renombra a .env)
# Proveedor recomendado: Gmail (con App Password) o SendGrid/Elastic Email

# Servidor SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true

# Credenciales
SMTP_USER=sanpe22dro1.1@gmail.com
SMTP_PASS=cwmaepzxlhkzrfyn

# Remitente
SMTP_FROM="Gestión Proyectos <no-reply@tudominio.com>"

# Otros (opcional)
# APP_BASE_URL=http://localhost:3000

# Base de datos MySQL (rellena según tu entorno)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=gestion_proyectos
DB_POOL_SIZE=10